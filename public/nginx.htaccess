location / {
   if (!-e $request_filename) {
   		rewrite  ^(.*)$  /index.php?s=/$1  last;
    }
}

# 解决静态文件访问404
location ~ ^/app/(?!admin\b)([a-zA-Z0-9_-]+)/static/(.*)$ {
    alias /www/wwwroot/name/app/$1/packages/static/$2;
}